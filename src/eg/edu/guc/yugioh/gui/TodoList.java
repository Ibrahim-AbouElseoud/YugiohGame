package eg.edu.guc.yugioh.gui;

public class TodoList {
/**
 * CHANGE THE MAIN MENU BUTTONS
 * JlayeredPane -> very useful
 * 
 * 
 * 
 * How to draw deck cards or do when they start do they automatically draw ? or do I create a deck and draw manually?
 * 
 * 
 * 
 * JPanel mainPanel = new JPanel();
            mainPanel.setLayout(new GridLayout(1, 3));

            buttonPanel = new JPanel();
            buttonPanel.setLayout(new GridLayout(12, 1));

            add(mainPanel);

            for (int i = 0; i < 12; i++) {
                buttonList.add(new Button("" + i));
                buttonPanel.add(buttonList.get(i));
                buttonList.get(i).addActionListener(this);
            }
            
                      remove decoractions and make them exit from the dedicated button
 *
 *
 */
}
